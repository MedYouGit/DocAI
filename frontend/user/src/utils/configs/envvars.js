// VITE_API_URL = import.meta.env.VITE_API_URL;

export const API_URL = import.meta.env.VITE_API_URL;
// get_envvars = (envvar_key) => {
//     validate_envvar_key(envvar_key)
//     if (envvar_key === "VITE_API_URL") {
//         return VITE_API_URL;
//     } else {
//         throw new Error(`Unknown envvar key: ${envvar_key}`);
//     }
//     }

// export default VITE_API_URL;